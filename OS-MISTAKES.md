# Feelings-OS 工程错误日志

> 哲学：**犯了两次及以上的错误都应该记录下来。**
> 规则：重复次数 ≥ 2 才入册。

---

## Rust 语言陷阱（来源：Axon / Anim 预埋）

### R01: tokio::Mutex guard 跨 `.await` 持锁死锁
- **解法**: `enum StateMachine` + `std::mem::replace`
- **重复次数**: 0（预埋）

### R02: anyhow::bail! 未导入直接调用
- **解法**: `use anyhow::bail;`
- **重复次数**: 0（预埋）

---

## Feelings-OS 特有陷阱

### O01: 在 Rust 上层写 volatile 寄存器操作
- **症状**: Rust 的安全模型不认 volatile 指针——编译器可能重排/优化掉你的寄存器写入
- **解法**: volatile 操作全部在 C 驱动层。Rust 上层通过 extern "C" fn + 共享帧缓冲区通信
- **重复次数**: 0（预埋）

### O02: 内存池 block 用了再 free——碎片
- **症状**: 不同大小的分配混在一起，运行 24h 后池子碎了
- **解法**: 每层 block 大小固定。4KB 永远只分配 4KB。不拆分不合并
- **重复次数**: 0（预埋）

### O03: 安全域中断处理里调了锁——死锁
- **症状**: P0 安域中断触发 → 处理函数试图获取 P1 持有的锁 → 死锁
- **解法**: 安域中断处理函数零锁。只读写 BRAM 端口 B 的固定寄存器
- **重复次数**: 0（预埋）
