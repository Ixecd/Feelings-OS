# Feelings-OS 开发哲学

> 性质：强制约定，不是建议
> 范围：Feelings-OS 仓库所有 Rust/C 代码、文档、提交
> 更新：随项目演进持续修订

---

## 一、Unix 哲学——六个小组件，各自只做一件事

每个守护进程只做一件事。做好。和其他进程通过标准文件接口通信。

```
mempoold    只管内存池的预分配和 block 回收。不管调度。
schedulerd  只管优先级抢占。不管内存。
busd        只管总线驱动和主时钟分发。不管缓存。
cached      只管四层缓存的物理映射。不管设备。
timerd      只管 PLL 锁相和全局时钟。不管总线。
logd        只管审计日志的追加写入。不管缓存。
```

组件之间不互相调用。它们通过 `/dev/*` 文件接口和共享帧缓冲区通信——和 Unix 管道完全相同，只是介质从字节流变成了固定帧缓冲页。

---

## 二、一切皆文件

硬件设备是文件。系统状态是文件。缓存指标是文件。不是比喻——`open / read / write / close` 操作和 Unix 文件系统语义完全一致。

```
file 操作         硬件对应                 行为
────────         ────────                 ────
open /dev/ear    获取耳后设备句柄         设备已连接 → 返回 fd。未连接 → -1
read /dev/ear    采集当前生理信号         返回固定大小帧缓冲——一帧一读
write /dev/ear   注入迷走神经刺激参数      写入成功后 FPGA 在下一拍执行
close /dev/ear   释放设备句柄             不影响设备继续运行（设备独立于 fd）
```

每个 `/dev` 文件是设备和用户态进程之间的唯一通道。不通过内核。不通过系统调用。fd 直接映射内存池内的帧缓冲页。

---

## 三、安全域永远最先

Feelings-OS 在公平和正确之间选择了正确。这是对 Unix CFS 的唯一偏离。

```
P0 安域     < 100μs 响应    硬件中断直接触发。不经过调度器
P1 感受域   1ms 帧级         被 P0 抢占——安域中断到达时立即切换
P2 设备 I/O  5ms             被 P0/P1 抢占
P3 后台      不限             只在 Idle 时运行

抢占规则
    任何 P0 事件到达 → 当前指令周期结束后立即切到安域处理
    不等到下一个调度片。不排队。不安检。
    安域处理完后 → 从中断点恢复被抢占的任务
```

这不是不公平——这是安全的需要。Unix 不知道哪条指令之后人的神经系统可能会收到错误信号。

---

## 四、零动态分配——启动时预分配全量内存

启动时 mempoold 拿了全部物理内存。运行期零 malloc。零 free。缺页中断不存在。

```
启动流程
    1. mempoold 启动 → 查询硬件物理内存总量 → 预分配 90% 为内存池
    2. 内存池分级：Page(4KB) / Chunk(64KB) / Slab(1MB) / Zone(16MB)
    3. 每层级的 block 大小相同 → 永不拆分合并 → 零外部碎片
    4. cached 启动 → 从内存池划出 L0 BRAM 分区 + L1/L2 区域
```

运行时分配 = 从对应层级的空闲链表中取一个 block。释放 = 归还到空闲链表。O(1)，无锁（或只在 L2 层有轻量锁）。

---

## 五、双层语言——Rust 上层 + C 驱动

```
Rust    六个守护进程——mempoold / schedulerd / busd / cached / timerd / logd
        /dev 文件接口层——open/read/write/close 实现
        帧缓冲区管理——mmap 零拷贝页切换
        零 unsafe——除非 FFI 到 C 驱动层

C       硬件驱动——ear.c / wrist.c / neck.c / temple.c / bus.c
        volatile 指针映射 FPGA 寄存器地址
        中断向量表配置——P0 安域中断优先级最高
        启动汇编——跳转到 Rust main 之前的初始化
```

C 写的那部分就是硬件寄存器操作——不需要 Rust 的抽象。Rust 写的那部分需要所有权模型来保证内存安全——不需要 C 的灵活。

---

## 六、禁止事项

```
❌ unsafe Rust——除非 FFI 边界到 C 驱动
❌ malloc / free——全部走内存池
❌ 动态线程创建——启动时固定线程数，运行时不变
❌ 内核态/用户态切换——单一地址空间
❌ 虚拟内存——物理连续，固定映射
❌ 标准库的 std::fs / std::net——不用 Linux 的文件系统和网络栈
❌ println!——走 tracing + logd
❌ 任何 Linux 系统调用——Feelings-OS 自己实现所有设备接口
```

---

*Feelings-OS 不是 Linux。是刚好够的那一层。*
