# OS-FUTURE.md — Feelings-OS 架构演化实验室

> 创建日期：2026-05-21
> 作用：存放高价值、高风险、需要"重新评估时机"的架构种子
> 关联：[ROADMAP.md](OS-ROADMAP.md)

---

## 种子库

| 编号 | 种子 | 状态 | 重新评估时机 |
|---|---|---|---|
| F1 | 多设备集群——多个主设备星型拓扑交叉覆盖 | 🌱 探索中 | v0.5+ 单设备稳定后 |
| F2 | Remote 缓存层——跨数据中心同步 | 探索中 | 多数据中心部署后 |
| F3 | 安全域硬件级隔离——独立 FPGA 芯片 | 探索中 | v1.0+ 裸机运行后 |

---

## F1: 多设备集群——多个主设备星型拓扑交叉覆盖

> 当前架构是单主设备（耳后-FPGA）星型拓扑。纺织物设备出现后，躯干可能需要第二个主设备作为区域 hub。

### 动机

纺织物 + 温控薄膜覆盖了躯干大面积区域。所有触觉点直接回传耳后主设备——总线带宽不够。需要躯干 hub 做区域预处理。

### 工程税

1. 主设备间时钟同步——两个 PLL 源，主-主架构
2. 总线的多 master 仲裁
3. 安全域在双主设备间的协同——一个主设备检测到异常，另一个必须同步进入 SafetyHold

---

## F2: Remote 缓存层——跨数据中心同步

> 当前 L3 是数据中心内分布式存储池。如果需要跨数据中心同步 Pattern Registry 更新和固件 OTA。

### 核心想法

Remote 层不参与实时管线。只做异步同步——Registry 更新分发、固件 OTA、SPL 账本镜像。

### 工程税

1. 跨地域网络延迟 10-100ms——只能异步
2. 共识——多个数据中心的 SPL 副本一致性

---

## F3: 安全域硬件级隔离——独立 FPGA 芯片

> 当前安全域和感受域共享同一块 FPGA，通过 BRAM 双端口隔离。极端场景下，感受域的硬件故障可能影响安全域的 BRAM 供电。

### 核心想法

安全域运行在独立 FPGA 芯片上。两块 FPGA 通过硬连线直连——感受域的输出必须经过安全域的校验才能到达设备。感受域 FPGA 完全无法访问安全域的 BRAM 和寄存器。

### 工程税

1. 硬件成本翻倍
2. 双芯片间的硬连线通信需要定义新协议
